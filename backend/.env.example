# Server Configuration
PORT=5001

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/novastra

# ============================================
# AI API Configuration
# ============================================
# The system now supports multiple AI providers with automatic fallback:
# 1. Groq (RECOMMENDED - Free tier with generous limits)
# 2. OpenAI (GPT-3.5/GPT-4)
# 3. Gemini (Google)
#
# Priority: Groq → OpenAI → Gemini (each with retries)
# ============================================

# Groq API (RECOMMENDED - Fast & Free!)
# Get your free API key at: https://console.groq.com/
GROQ_API_KEY=

# OpenAI API (Alternative)
# Get your API key at: https://platform.openai.com/api-keys
AI_API_KEY=

# AI Provider Selection (optional - defaults to Groq)
# Options: 'groq', 'openai', 'gemini'
AI_PROVIDER=groq

# ============================================
# Notes:
# ============================================
# - Groq offers Llama3 and Mixtral models with free tier
# - If no API keys are set, the system will use local rules-based fallback
# - The local fallback generates learning paths without any API calls
# ============================================

